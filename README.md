📚 Library Management System

A full-stack Library Management System built using React (Frontend) and Node.js + Express + MongoDB (Backend).
It allows users to manage books, handle authentication, and perform CRUD operations easily.
The project includes separate frontend and backend folders for clean structure and scalability.